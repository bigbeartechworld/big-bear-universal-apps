# Configuration for dockhand setup

# Name of the big-bear-dockhand application
name: big-bear-dockhand

# Service definitions for the big-bear-dockhand application
services:
  # Service name: app
  # The main Dockhand application service
  app:
    # Name of the container
    container_name: big-bear-dockhand

    # Image to be used for the container
    image: fnsys/dockhand:latest@sha256:d6e0c277fd9e3ddd0039256bf1e74510f3352cd3237bd9c7c4b30eeebb7af7ba

    # Container restart policy
    restart: unless-stopped

    # Volumes to be mounted to the container
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # Docker socket access
      - dockhand_data:/app/data                     # Application data

    # Ports mapping between host and container
    ports:
      - "3003:3000"

# Volumes definition for the big-bear-dockhand application
volumes:
  dockhand_data:
    name: dockhand_data
    driver: local
