# Name of the application
name: big-bear-umami
# List of services that this application uses
services:
  # Main service for the Umami application
  app:
    image: ghcr.io/umami-software/umami:postgresql-latest@sha256:fa32d116cf20cad52cbc3fad9a63b46e7fa02299d8f967168eb453d49c476b4a # Docker image for the Umami app
    ports:
      - "3000:3000" # Port mapping: host:container
    environment: # Environment variables for the service
      DATABASE_URL: postgresql://umami:umami@db:5432/umami
      DATABASE_TYPE: postgresql
      APP_SECRET: replace-me-with-a-random-string
    depends_on: # Dependency of this service
      db:
        condition: service_healthy
    restart: always # Restart policy
  # Database service for the Umami application
  db:
    image: postgres:15-alpine@sha256:3d0f7584ed7d04e27fa050d6683a74746608faf21f202be78460d679cc56461f # Docker image for the PostgreSQL database
    environment: # Environment variables for the service
      POSTGRES_DB: umami
      POSTGRES_USER: umami
      POSTGRES_PASSWORD: umami
    volumes: # Volumes for data persistence
      - umami_pgdata:/var/lib/postgresql/data
    restart: always # Restart policy
    healthcheck: # Health check configuration
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
volumes:
  umami_pgdata:
    name: umami_pgdata
    driver: local
