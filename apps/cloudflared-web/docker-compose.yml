# Configuration for cloudflared-web setup

# Name of the big-bear-cloudflared-web application
name: big-bear-cloudflared-web
# Service definitions for the big-bear-cloudflared-web application
services:
  # Service name: big-bear-cloudflared-web
  # The `big-bear-cloudflared-web` service definition
  big-bear-cloudflared-web:
    # Name of the container
    container_name: big-bear-cloudflared-web
    # Image to be used for the container
    image: wisdomsky/cloudflared-web:2026.8.2@sha256:e30a66ce139f5abe11e9a057c3b22e2832caa244232d6a4c61fd2510a9657bee
    # Container restart policy
    restart: unless-stopped
    # Network mode
    network_mode: host
    # Environment variables for the container
    environment:
      - WEBUI_PORT=14333
      - EDGE_IP_VERSION=auto
      - PROTOCOL=auto
      - METRICS_ENABLE=false
      - METRICS_PORT=60123
      - BASIC_AUTH_USER=admin
      - BASIC_AUTH_PASS=""
    # Volumes for the service
    volumes:
      - cloudflared-web_config:/config

# Volumes used by the application
volumes:
  cloudflared-web_config:
    name: cloudflared-web_config
    driver: local
